<h1>Editing Book</h1>
<%= form_with model: @book, url: update_book_path(@book.id), method: :patch, local:true do |f| %>

<div  class = "field">
 <ladel>Title</ladel>
  <%= f.text_field:title %>
</div>

<div  class = "field">
 <ladel>Body</ladel>
 <%= f.text_area :body %>
</div>

<div = class= "action">
<%= f.submit " Updeta Book " %>
</div>

<%= link_to "Show", book_path(@book.id) %>
|
<%= link_to "Back", books_path %>
<% end %>

